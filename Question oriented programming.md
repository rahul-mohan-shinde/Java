Bahut badhiya approach hai! **Question-Based Learning** se tumhara concept clear bhi hoga aur interview bhi crack kar paoge. Main tumhe har topic ke liye **most important interview questions** dunga jo actual companies mein puchhe jaate hain.

## 📌 Month 1: Core Java Questions

### Variables, Data Types & Operators
1. Primitive aur Non-primitive data types mein kya difference hai?
2. Type casting ke types kya hain? Implicit aur Explicit casting explain karo.
3. `==` aur `.equals()` mein kya difference hai?
4. Logical operators (&&, ||) aur Bitwise operators (&, |) mein difference?
5. What is the output of `System.out.println(10 + 20 + "Hello")`? Kyun?

### Control Statements & Arrays
6. Switch statement mein `break` kyun use karte hain? Fall-through kya hai?
7. For-each loop ka use kab karte hain? Limitation kya hai?
8. 2D array ka memory representation kya hai?
9. ArrayIndexOutOfBoundsException kyun aati hai? How to avoid?

### Methods & OOP Basics
10. Method Overloading kaise achieve karte hain? Return type change kar sakte hain?
11. Constructor chaining kya hai? `this()` aur `super()` ka use?
12. Encapsulation achieve kaise karte hain? Example do.
13. Multiple inheritance kyun support nahi karta Java? Interface se kaise solve karte hain?
14. Abstract class aur Interface mein difference? Kab kya use karein?
15. Access modifiers: private, default, protected, public - inka scope kya hai?

### String & Exception Handling
16. String immutable kyun hai? Benefits?
17. `String`, `StringBuilder`, `StringBuffer` mein difference? Performance kaunsa best hai?
18. `try-catch-finally` ka flow kya hai? `finally` kab execute nahi hota?
19. Checked vs Unchecked Exception? Example do.
20. Custom Exception kaise banate hain? `throws` vs `throw`?

---

## 📌 Month 2: Collections + Multithreading + SQL

### Collections Framework
21. `ArrayList` vs `LinkedList` - performance comparison? Kab kya use karein?
22. `HashMap` internal working? `equals()` aur `hashCode()` ka role?
23. `HashSet` duplicates kaise detect karta hai?
24. `TreeMap` vs `HashMap` - difference? Sorting kaise hoti hai?
25. `Comparable` vs `Comparator` - kab kya use karein? Real example do.
26. `ConcurrentModificationException` kyun aati hai? Solution kya hai?

### Multithreading
27. `Thread` class vs `Runnable` interface - better kya hai aur kyun?
28. Thread lifecycle states batao.
29. Synchronization kyun chahiye? `synchronized` keyword kaise kaam karta hai?
30. Deadlock kya hai? Example do aur avoid kaise karein?
31. `ExecutorService` ka kya benefit hai `Thread` class se?
32. `Callable` vs `Runnable` - difference? `Future` kya hai?

### Java 8 Features
33. Lambda expression kya hai? Functional interface se kaise relate hai?
34. Stream API kya hai? `map()`, `filter()`, `reduce()` ka use?
35. `Optional` kyun use karte hain? `null` pointer exception se bachata hai kaise?

### SQL
36. SQL joins explain karo - INNER, LEFT, RIGHT, FULL with example?
37. `GROUP BY` vs `ORDER BY` - difference?
38. `HAVING` vs `WHERE` - kab kya use karein?
39. Subquery vs Join - better kaunsa hai aur kyun?
40. Index kya hai? Types aur benefits? Performance par kaise effect padta hai?
41. Normalization kya hai? 1NF, 2NF, 3NF explain karo.

---

## 📌 Month 3: JDBC + Spring Core + Spring Boot

### JDBC
42. JDBC steps batao - connection se lekar data fetch tak.
43. `Statement` vs `PreparedStatement` - better kaunsa hai aur kyun?
44. SQL Injection kya hai? `PreparedStatement` kaise bachata hai?
45. Transaction management in JDBC? `commit()` aur `rollback()`?

### Spring Core
46. IoC (Inversion of Control) kya hai? Spring mein kaise implement hota hai?
47. Dependency Injection types - Constructor vs Setter vs Field injection?
48. `@Component`, `@Service`, `@Repository`, `@Controller` - difference?
49. Bean scope types - Singleton vs Prototype vs Request vs Session?
50. `@Autowired` kaise kaam karta hai? `@Qualifier` kab use karein?

### Spring Boot
51. Spring Boot auto-configuration kaise kaam karta hai?
52. `application.properties` vs `application.yml` - difference?
53. Spring Boot layered architecture explain karo - Controller, Service, Repository.
54. `@RestController` vs `@Controller` - difference?
55. Validation kaise karte hain? `@Valid`, `@NotNull`, `@Email` ka use?
56. Global exception handling kaise karein? `@ControllerAdvice`?

---

## 📌 Month 4: REST API + JPA + Security

### REST API
57. REST API principles kya hain? (Stateless, Cacheable, Uniform Interface etc.)
58. `@RequestMapping` vs `@GetMapping` - difference?
59. `@PathVariable` vs `@RequestParam` - kab kya use karein?
60. `ResponseEntity` kyun use karte hain? Kya benefit hai?
61. DTO kya hai? Kyo use karte hain? Entity se alag kyun rakhte hain?
62. HTTP status codes - 200, 201, 400, 401, 403, 404, 500 explain karo.

### JPA/Hibernate
63. JPA vs Hibernate - difference? ORM kya hai?
64. Entity relationships - `@OneToMany`, `@ManyToOne` - mapping kaise karein?
65. Lazy vs Eager loading - difference? Performance par kaise effect?
66. N+1 query problem kya hai? Solution?
67. JPQL vs Native SQL - better kaunsa hai?
68. Pagination kaise implement karte hain? `Pageable` interface?

### Spring Security
69. Authentication vs Authorization - difference?
70. `BCryptPasswordEncoder` kya hai? Password store kaise karte hain?
71. JWT (JSON Web Token) structure kya hai? Header, Payload, Signature?
72. JWT authentication flow explain karo - login se lekar request tak.
73. Role-based access control kaise implement karte hain? `@PreAuthorize`?
74. Spring Security filter chain kaise kaam karta hai?

---

## 📌 Month 5: DSA + Git + Projects

### DSA (Interview Most Asked)
75. Find missing number in array (1 to n).
76. Reverse a string without using inbuilt function.
77. Find duplicate elements in array.
78. Check if string is palindrome.
79. Find middle element of linked list in single pass.
80. Implement stack using array.
81. Check balanced parentheses using stack.
82. Find first non-repeating character in string.
83. Two sum problem - find pair with given sum.
84. Binary search implementation.
85. Merge two sorted arrays.
86. Find factorial using recursion.
87. Time complexity - O(1), O(n), O(n²), O(log n) examples do.

### Git & Maven
88. Git workflow - `add`, `commit`, `push`, `pull`, `merge` explain karo.
89. Branching strategy - feature branch, hotfix, release branch.
90. `pom.xml` structure - dependencies, plugins, properties.
91. Maven lifecycle phases - compile, test, package, install.

---

## 📌 Project-Based Questions (Most Important)

### Employee Management System
92. Employee aur Department ka relation kaise map karein? (JPA)
93. Employee search by name, department, salary kaise karein? (JPQL)
94. Pagination aur sorting kaise implement karein?

### Library Management System
95. Book issue/return ka logic kaise implement karein? (Transaction management)
96. Fine calculation kaise karein agar book late return ho?

### WorkNest (Resume Project - Most Important)
97. JWT authentication flow kaise implement kiya?
98. Role-based access (ADMIN, USER, MANAGER) kaise handle kiya?
99. Task aur Workspace relationships kaise map ki?
100. Exception handling kaise kiya? Global handler?
101. Database schema design kya hai? (Tables and relationships)
102. REST APIs ki list do with endpoints.
103. Security kaise implement ki? (JWT + Spring Security)
104. Frontend (Angular) ke saath integration kaise kiya?
105. Deployment kahan kiya? (AWS, Azure, or Heroku?)

---

## 🎯 Interview Tips

### 50 Most Important Questions for Fresher Interview
1. Java features batao.
2. OOP principles.
3. Exception handling types.
4. Collections hierarchy.
5. HashMap internal working.
6. Multithreading vs multitasking.
7. Spring Boot vs Spring.
8. REST API principles.
9. SQL joins with example.
10. JPA relationships.
11. JWT authentication.
12. DSA - 2-3 basic problems solve karo.
13. Your project explanation with challenges.
14. Agile methodology kya hai?
15. SDLC phases.

---

## 🔥 Daily Practice Schedule

| Time | Topic |
|------|-------|
| 2 hrs | DSA problems (2-3 problems daily) |
| 2 hrs | Core Java + Spring Boot concepts |
| 1 hr | SQL queries practice |
| 1 hr | Project development |

---

## 📚 Resources Recommended
- **Core Java**: CodeWithHarry, Telusko
- **Spring Boot**: Java Brains, Amigoscode
- **DSA**: LeetCode Easy + Medium
- **SQL**: W3Schools, SQLZoo
- **Projects**: GitHub search "Spring Boot projects"

---

Yeh saare questions practice karo, concepts clear karo, aur **2-3 projects** build karo. 5 months mein tum job-ready ho jaaoge! 🚀

Kisi specific topic ke liye detailed answer chahiye toh batao, main deeply explain karunga. All the best! 💪
